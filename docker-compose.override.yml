version: '3.8'

services:
  app:
    # Development overrides
    environment:
      - NODE_ENV=development
    volumes:
      - ./app:/app  # Read-write for development
      - /app/node_modules  # Preserve node_modules in container
    ports:
      - "9229:9229"  # Node.js debug port